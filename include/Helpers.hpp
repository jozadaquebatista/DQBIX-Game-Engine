#ifndef __HELPERS__
#define __HELPERS__
#pragma once

#include <string>
#include <fstream>
#include <streambuf>

std::string readFile(std::string filepath);
//
int stringToInt(std::string text);
#endif
